#ifndef MMM_STATISTIC_HPP
#define MMM_STATISTIC_HPP

#include <cstddef>

namespace mstat {
inline size_t gl_calls{0};
inline size_t draw_calls{0};
};  // namespace mstat

#endif  // MMM_STATISTIC_HPP
